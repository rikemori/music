ここに YouTabs で書き出した TAB 譜ファイルを置きます（ファイル名は自由です）。

  .gp / .gpx / .gp5 / .musicxml / .mxl / .xml が使えます。
  ギターとベースは、ファイルの中のパート名や音色から自動で判別します。
  楽器ごとに別ファイルでも、1つにまとまっていてもOKです。

  歌詞の .txt（名前は自由。README以外の .txt を歌詞として使います）
               歌詞を1行ずつ書いたテキスト（任意）。右の歌詞パネルに表示されます（空行は行間になります）。
